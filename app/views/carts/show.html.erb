<!-- Nombre d'articles -->
<%= @cart_items.length%>

<!-- Iteration sur les articles du panier-->
<% @items.each_with_index do |item, i|%>
  <!-- Pour item n°x -->
  <%= i+1 %>
  <%= item.title%>
  <%= item.price%>
<% end %>
