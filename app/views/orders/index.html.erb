<!-- short -->
        <br><br>
        <div class="using-border py-3">
            <div class="inner_breadcrumb  ml-4">
                <ul class="short_ls">
                <li><h4>Your Orders</h4></li>
                </ul>
            </div>
        </div>
        <br><br>
<!-- //short-->

<section class="checkout py-lg-4 py-md-3 py-sm-3 py-3">
  <div class="container py-lg-5 py-md-4 py-sm-4 py-3">
    <div class="shop_inner_inf">
      <div class="privacy about">
        <h3>Orders</h3>
        <div class="checkout-right">
          <h4>Your passed orders:</h4>
          <table class="timetable_sub">

            <thead>
              <tr>
                <th>SL No.</th>
                <th>Invoice Number</th>
                <th>Amount</th>
              </tr>
            </thead>

            <tbody>
              <% i=0 %>
              <% @orders.each do |order|%>
              <tr class="rem1">
                <td class="invert"><%= i += 1 %></td>
                <td class="invert"><%= order.id %></td>
                <td class="invert">
                  <% ordered_items = OrderedItem.where(order_id: order.id) %>
                  <% amount = 0 %>
                  <% ordered_items.each do |item| %>
                    <% amount += Item.find(item.item_id).price * item.quantity %>
                    <%= amount %> €
                  <% end %>
                </td>
                <td class="invert"><%= link_to "See more", order_path(order.id), style: 'color:#ff3b9f' %></td>
              </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</section>

