<div class='container'>
	<div class='card-deck'>
		<div class="row">

			<% @items.each do |item| %>
				<div class="card border-primary mb-3" style="min-width: 20rem;">
					<%= item.title %>
					<%= item.description %>
					<%= item.price %>
					<%= item.image_url %>
					<%= link_to "Show", item_path(item.id), class: "btn-primary"%>
				</div>
			<% end %>
		</div>
	</div>
</div>




