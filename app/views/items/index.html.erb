
<!-- banner -->

    <div class="inner_page-banner one-img">
    </div>
    <!--//banner -->
<div class="container">
    <div class="card-columns">
        <% @items.each do |item| %>
            <div class="card">
            <% if item.picture.attached? == false%>
                <img src=<%= item.image_url %> class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title"><%= item.title %></h5>
                    <p class="card-text">€ <%= item.price %></p>
                </div>
                <div class="card-footer text-center">
                    <button type="submit" class="toys-cart ptoys-cart add">
                                Shop Now
                    </button>
                    <hr>
                    <button type="submit" class="toys-cart ptoys-cart add">
                    <%= link_to "More details", item_path(item.id) %>
                    </button>
                    <% else %>
                    <%= image_tag item.picture, alt: 'picture', class: "card-img-top" %>
                    <% end %>
                </div>
            </div>
            <br>
            <br>
        <% end %>
    </div>
</div>        
<br>